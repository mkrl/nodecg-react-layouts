{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"type": "array",
  "description": "List of all layouts that are available",
  "items": {
    "type": "object",
    "properties": {
      "label": {
        "type": "string",
        "default": "Standby/Idle",
        "description": "A layout name that humans can understand"
      },
      "value": {
        "type": "string",
        "default": "standby",
        "description": "An url-safe, kebab/camel case layout name for internal usage"
      }
    },
    "additionalProperties": false,
    "required": [
      "label",
      "value"
    ]
  }
}

